# 2026-09-21

## EmbedSkills 仓 README 重写（GitHub 风格 · 中文）

**背景**：提交 `e6d337f` 在整理 skill 时把旧 README.md（2 行）与 LICENSE 一并删了，README 缺失。

**产出**：`README.md`（280 行 / 17393 字节），本地 commit `6830049`，**未推送**（用户自行 push）。

**结构与依据**：
- 项目定位段 → 「不是代码库，是经验的可执行封装」，配**三条准入门槛**（可判据 / 可复现 / 有出处）
  + 隐含第四条「只写反直觉的部分」。
- 仓库结构段 → 三层加载模型表（L1 常驻 description ≈3 KB / L2 SKILL.md 按需 / L3 references 精确）。
- 框架图 → L0 方法论 `mcu-ai-agent-workflow` → 建(`stm32-build-and-scaffold`/`embedded-windows-toolchain`)
  / 验(`embedded-verification-acceptance`/`embedded-config-portal`) / 查(`mcu-debug-forensics`/`truth-source-and-model-vendoring`)
  → 平台领域层。
- Skill 清单 → 18 个全部列出，按域分组（MCU 8 / GUI 1 / 平台移植 2 / SOC 1 / 机器人 3 / 前端 1 / 工具 2），
  每个附「典型触发词」，全部带 SKILL.md 内链。
- 跨 Skill 复用链 → 5 条常用组合（从零做 MCU 工程 / 板子像死了 / 屏显示不对 / 加网页配置 / 改机构参数前）。
- 适用人群 → ✅强烈适合 / ⚠️部分适合 / ❌不适合 三级。
- 规划 → 已完成（09-21 整理 39→18、description 26KB→3KB）+ 计划四项。
- 维护约定 + License(MIT)。

**核对过的数字**（均来自实测，非估算）：

| 指标 | 实测 |
|---|---|
| Skill 目录数 | 18（`ls */SKILL.md`） |
| references 篇数 | 68（`ls */references/*.md`） |
| SKILL.md 总行数 | 926 |
| references 总行数 | 7650 |
| 仓库体积（不含 .git） | 752K |
| 仓 ↔ `~/.workbuddy/skills/` | 18/18 目录 `diff -rq` **全部 SAME** |

**校验方法**（可复用）：README 内 18 个 `SKILL.md` 链接逐个 `test -f`，零断链。

**注意**：Git Bash 下 `grep -o '](\(...\)'` 的 BRE 分组、以及 `sort -u` 在本机 PATH 上会静默失效
（前者返回 0 条、后者报「系统找不到指定的文件」）。改用 `tr '(' '\n' | grep | sort -u` 才拿到真实结果 ——
统计类命令的结果必须交叉验证，否则会得到「零断链」的假象。

**遗留 / 待办**：
- `LICENSE` 文件仍缺失，但 README 末尾已写 MIT 声明 → 需要时补回（内容见 `git show e6d337f^:LICENSE`）。
- `support_tools/sync_skills.sh` 在工程侧、未随仓；镜像白名单写死在脚本里。
